import React from 'react';
import ReactDOM from 'react-dom';
import Convert from './convert';

document.addEventListener("DOMContentLoaded", () => {
	const root = document.getElementById("root");
	ReactDOM.render(<Convert/>, root);
});
